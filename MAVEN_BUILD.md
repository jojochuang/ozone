# Maven build (current release path)

Apache Ozone still uses Maven for ASF releases and the majority of CI jobs.

Bazel migration progress is documented in [`tools/bazel/MIGRATION_STATUS.md`](tools/bazel/MIGRATION_STATUS.md).
Do not remove this file or the `pom.xml` tree until the PMC approves a Bazel-only release candidate.
