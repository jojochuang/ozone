# HDDS-9714 fork PR: CodeFactor compares against a stale default branch and reports hundreds of
# pre-existing module issues. CI already runs checkstyle, PMD, SpotBugs, and RAT on Java sources.
version: 1
exclude:
  - "**/*"
